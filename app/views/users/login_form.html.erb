<%= form_with url: "/login", method: :post do |f| %>
  <%= f.label "Email:" %>
  <%= f.textfield :email %>
  <%= f.label "Password:" %>
  <%= f.password_field :password %>
  <%= f.submit "Login" %>
<% end %>